import React, { useEffect, useState } from "react";

type CreateCardFormProps = {};

export const CreateCardForm = ({}: CreateCardFormProps) => {
  const [state, setState] = useState();

  useEffect(() => {}, []);

  return <></>;
};
